<li>
	<span class="content"><%= micropost.content %></span>
	<% if micropost.image.present? %>
		<span class="image"><%= image_tag micropost.image.url(:medium) %></span>
	<% end %>
	<span class="timestamp">Posted <%= time_ago_in_words(micropost.created_at) %> 전.</span>
</li>